{
  "name": "booking-cron",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build required'",
    "start": "echo 'Use send-reminders or send-post-dinner scripts'",
    "send-reminders": "npx tsx send-reminders.ts",
    "send-reminders:dry-run": "npx tsx send-reminders.ts --dry-run",
    "send-post-dinner": "npx tsx send-post-dinner.ts",
    "send-post-dinner:dry-run": "npx tsx send-post-dinner.ts --dry-run"
  },
  "dependencies": {
    "pg": "^8.20.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
